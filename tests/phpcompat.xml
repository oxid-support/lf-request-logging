<?xml version="1.0"?>
<ruleset name="Heartbeat PHP Compatibility">
    <description>
        PHP version compatibility check for the OXID 6.5 line.
        Single source of truth for the supported PHP range; CI and the local
        `composer phpcompat` script both consume this file.
    </description>

    <!-- Module source only -->
    <file>../src/</file>
    <arg name="extensions" value="php"/>
    <arg value="p"/>
    <arg name="colors"/>

    <!-- Supported PHP range for this line -->
    <config name="testVersion" value="7.4-8.1"/>
    <rule ref="PHPCompatibility"/>
</ruleset>
